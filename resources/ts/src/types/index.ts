export type RoutesType = {
    dashboard: string;
    myPage: string;
    threadHistory: string;
    profileShow: string;
    login: string;
    logout: string;
    register: string;
};

export type UserType = {
    name: string;
};
